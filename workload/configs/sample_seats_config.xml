<?xml version="1.0"?>
<!-- This is a sample configuration file for the SEATS workload. -->
<!-- The configuration file can be found at https://github.com/cmu-db/benchbase/blob/main/config/postgres/sample_seats_config.xml. -->
<parameters>

    <!-- Connection details -->
    <type>POSTGRES</type>
    <driver>org.postgresql.Driver</driver>
    <url>connection_string</url>
    <username>dbuser</username>
    <password>dbpassword</password>
    <reconnectOnConnectionFailure>true</reconnectOnConnectionFailure>
    <isolation>TRANSACTION_SERIALIZABLE</isolation>
    <batchsize>128</batchsize>

    <!-- Scalefactor in SEATS scales by *1000 the number of customers -->
    <scalefactor>1</scalefactor>

    <!-- The workload -->
    <terminals>1</terminals>
    <works>
        <work>
            <time>86400</time>
            <rate>unlimited</rate>
            <weights>10,10,35,35,5,5</weights>
        </work>
    </works>

    <!-- SEATS Procedures Declaration -->
    <transactiontypes>
        <transactiontype>
            <name>DeleteReservation</name>
        </transactiontype>
        <transactiontype>
            <name>FindFlights</name>
        </transactiontype>
        <transactiontype>
            <name>FindOpenSeats</name>
        </transactiontype>
        <transactiontype>
            <name>NewReservation</name>
        </transactiontype>
        <transactiontype>
            <name>UpdateCustomer</name>
        </transactiontype>
        <transactiontype>
            <name>UpdateReservation</name>
        </transactiontype>
    </transactiontypes>
</parameters>
